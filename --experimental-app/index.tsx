// This file was a leftover client entry (CRA-style) that referenced a
// non-existent `./App` module and caused TypeScript build errors.
// Next.js (app router) does not use this file — keep it empty so tooling
// that scans the project won't fail during `npm run build`.

export {};